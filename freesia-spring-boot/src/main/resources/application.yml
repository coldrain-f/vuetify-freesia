spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  database:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  jpa:
      hibernate:
        ddl-auto: create
logging:
  level:
    org:
      hibernate:
        SQL: debug